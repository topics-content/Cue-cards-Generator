import { CostObservatory } from "@/components/CostObservatory";

// Open to everyone who can sign in (the (app) layout and middleware already require a Scaler account).
export default function CostObservatoryPage() {
  return <CostObservatory />;
}
